@{
  Layout = "_Layout";
}

@using PierresTreats.Models
@model PierresTreats.Models.Treat

<h1>Add New Treat</h1>

<div class="container page-content">
  @using (Html.BeginForm())
  {
    <div class="form-group form-inline">
      @Html.LabelFor(model => model.Name, new{@class = "col-sm-4 col-form-label"})
      @Html.TextBoxFor(model => model.Name, new {@class = "form-control", required="required"})
    </div>
    <div class="form-group form-inline">
      @Html.LabelFor(model => model.Description, new{@class = "col-sm-4 col-form-label"})
      @Html.TextBoxFor(model => model.Description, new {@class = "form-control", required="required"})
    </div>
    <div class="form-group form-inline">
      @Html.LabelFor(model => model.Price, new{@class = "col-sm-4 col-form-label"})
      @Html.TextBoxFor(model => model.Price, new {@class = "form-control", required="required"})
    </div>

    <div>
      <p>Flavor Profile:</p>
      @if (@ViewBag.Flavors.Count == 0)
      {
        <p>No Flavors Selections Added</p>
      }
      else
      {
        <ul>
          @foreach (Flavor flavor in ViewBag.Flavors)
          {
            <li>
              <input type="checkbox" name="flavorIds" value="@flavor.FlavorId" />
              @flavor.Name
            </li>
          }
        </ul>
      }
    </div>

    <div class="text-center my-3">
      <input type="submit" value="Add" class="btn btn-primary btn-lg" />
    </div>
  }
</div>